<div class="cartouche">

	<h3>Title</h3>

	{{form_row(numberForm.title)}}

	{{form_row(numberForm.film)}}
	{# {% if filmId %}
		numberForm.film = filmId
	{% elseif filmId is null %} #}
	{# {{form_row(numberForm.film)}} #}
	{# 	{% endif %} #}

	{# {{form_row(numberForm.film)}} #}

	{{ form_widget(numberForm.completeTitle) }}
	{{ form_label(numberForm.completeTitle, "Completed?") }}
	{{ form_errors(numberForm.completeTitle) }}

	{{form_row(numberForm.validationTitle)}}
</div>

{# ok pour title sauf pb avec sélection auto de film  -
todo : selction auto de film quand new number
todo : validation
#}

<div class="cartouche">

	<h3>Outlines</h3>

	{{form_row(numberForm.beginTc, {
		'label' : "Begin timecode"
	})}}
	{{form_row(numberForm.endTc, {
		'label' : "End timecode"
	})}}
	{{form_row(numberForm.beginThesaurus,{ 
		'label' : "Begin (from thesaurus) (1 choice) (mandatory)"
	})}}
	{{form_row(numberForm.endingThesaurus,{ 
		'label' : "Ending (from thesaurus) (1 choice) (mandatory)"
	})}}
	{{ form_row(numberForm.completenessThesaurus, {
		'label' : "Is the number closed and \"satisfying\"? (from thesaurus) (1 choice) (mandatory)"
	}) }}
	{{ form_row(numberForm.completOptions, {
		'label' : "Is the number interrupted and are there reprises? (from thesaurus) (1 choice only for the moment) (optional)"
	}) }}

	{{ form_widget(numberForm.completeTc) }}
	{{ form_label(numberForm.completeTc, "Completed?") }}
	{{ form_errors(numberForm.completeTc) }}


	{{form_row(numberForm.validationTc)}}
</div>

{# ok pour outlines sauf
todo : many to many completOptions
todo : validation
#}

<div class="cartouche">

	<h3>Structure</h3>

	{{form_row(numberForm.structure, {
		'label' : "Structure (from thesaurus) (1 choice) (mandatory)"
	})}}

	{{ form_widget(numberForm.completeStructure) }}
	{{ form_label(numberForm.completeStructure, "Completed?") }}
	{{ form_errors(numberForm.completeStructure) }}
	{{form_row(numberForm.validationStructure)}}

</div>

{# ok pour structure sauf
todo : validation
#}

<div class="cartouche">
	<h3>Shots</h3>

	{{form_row(numberForm.shots, {
		'label' : "Number of shots"
	})}}
	{{ form_widget(numberForm.completeShots) }}
	{{ form_label(numberForm.completeShots, "Completed?") }}
	{{ form_errors(numberForm.completeShots) }}
	{{form_row(numberForm.validationShots)}}

</div>

{# ok pour shots sauf
todo : validation
#}

<div class="cartouche">

	<h3>Performers</h3>

	{{form_row(numberForm.performance_thesaurus, {
		'label' : "Performance type (from thesaurus) (1 choice) (mandatory)"
	})}}

	{{ form_row(numberForm.performers, {
		'label' : "Performers (multi)"
	})}}
	<a href="{{ path('editorNewPerson') }}" target="_blank" title="Create a new person if not already in the database">add a person</a>


	{{ form_row(numberForm.figurants, {
		'label' : "Stars who don't participate (ex : as spectators) (multi)"
	}) }}
	<a href="{{ path('editorNewPerson') }}" target="_blank" title="Create a new person if not already in the database">add a person</a>

	{{ form_widget(numberForm.completePerformance) }}
	{{ form_label(numberForm.completePerformance, "Completed?") }}
	{{ form_errors(numberForm.completePerformance) }}
	{{form_row(numberForm.validationPerformance)}}

</div>

{# ok pour Performers sauf
todo : validation
todo : créer une fonction ordre alpha dans repository pour les persons by name?
#}

<div class="cartouche">

	<h3>Backstage & integration</h3>

	{{form_row(numberForm.spectators_thesaurus, {
		'label' : "Are there spectators within the scene ? + Is there a direct adress (from thesaurus) (1 choice) (mandatory)"
	})}}
	{{form_row(numberForm.diegetic_thesaurus,{
		'label' : "Is this an onstage number or a diegetic performance ? (from thesaurus) (1 choice) (mandatory)"
	} )}}
	{{form_row(numberForm.musician_thesaurus, {
		'label' : "Are there visible musicians? (from thesaurus) (1 choice) (mandatory)"
	})}}
	{#{{form_row(numberForm.integration)}}#}
	{{form_row(numberForm.integration_thesaurus, {
		'label' : "Integration (from thesaurus) (1 choice) (mandatory)"
	})}}
	{{form_row(numberForm.integoptions, {
		'label' : "Integration : specific effects (from thesaurus) (multi) (optional)"
	})}}

	{{ form_widget(numberForm.completeBackstage) }}
	{{ form_label(numberForm.completeBackstage, "Completed?") }}
	{{ form_errors(numberForm.completeBackstage) }}
	{{form_row(numberForm.validationBackstage)}}

</div>

{#
ok sauf
todo : integoptions (plusieurs possibles - many to many)
todo : validation
#}

<div class="cartouche">

	<h3>Themes</h3>

	{{form_row(numberForm.costumes, {
		'label' : "Costumes (from thesaurus) (multi) (mandatory)"
	})}}

	{{ form_row(numberForm.stereotype, {
		'label' : "Ethnic stereotypes : is there any ethnic impersonation (accent,  make up, ...)? (from thesaurus) (multi)"
	}) }}
	{#{{ form_row(numberForm.diegeticPlace) }}#}
	{#{{ form_row(numberForm.socialPlace) }}#}
	{{ form_row(numberForm.diegetic_place_thesaurus, {
		'label' : "Diegetic place (from thesaurus) (multi) (mandatory)"
	}) }}
	{{ form_row(numberForm.general_localisation, {
		"label" : "General localisation (from thesaurus) (multi) (optional)"
	}) }}
	{{form_row(numberForm.imaginary, {
		'label' : "Imaginary place (from thesaurus) (multi) (optional)"
	})}}
	{#{{form_row(numberForm.exoticism)}}#}
	{{form_row(numberForm.exoticism_thesaurus, {
		"label" : "General exoticism (linked to dancing styles, settings, ...) (from thesaurus) (multi) (optional)"
	})}}
	{{ form_widget(numberForm.completeTheme) }}
	{{ form_label(numberForm.completeTheme, "Completed?") }}
	{{ form_errors(numberForm.completeTheme) }}

	{{form_row(numberForm.validationTheme)}}
</div>

<div class="cartouche">

	<h3>Tone</h3>
	{#
	{{form_row(numberForm.mood_thesaurus, {
		'label' : "Mood (from thesaurus)"
	})}}
	#}
	{{ form_row(numberForm.general_mood) }}
	{{ form_row(numberForm.genre) }}

	{{ form_widget(numberForm.completeMood) }}
	{{ form_label(numberForm.completeMood, "Completed?") }}
	{{ form_errors(numberForm.completeMood) }}
	{{form_row(numberForm.validationMood)}}
</div>

<div class="cartouche">

	<h3>Dance</h3>

	{{form_row(numberForm.choregraphers, {
		'label' : "Choregraphers"
	})}}
	<a href="{{ path('editorNewPerson') }}" target="_blank" title="Create a new person if not already in the database">add a person</a>


	{{form_row(numberForm.dancemble, {
		"label" : "Dancing ensemble (from thesaurus) (multi) (mandatory)"
	})}}
	{{form_row(numberForm.dancingType)}}
	{{form_row(numberForm.danceSubgenre)}}
	{{form_row(numberForm.danceContent)}}

	{{ form_widget(numberForm.completeDance) }}
	{{ form_label(numberForm.completeDance, "Completed?") }}
	{{ form_errors(numberForm.completeDance) }}

	{{form_row(numberForm.validationDance)}}
</div>

<div class="cartouche">

	<h3>Music</h3>

	{{form_row(numberForm.song)}}
	{{form_row(numberForm.musensemble)}}
	{{form_row(numberForm.dubbing)}}
	{{form_row(numberForm.tempo_thesaurus)}}

	{{form_row(numberForm.musical, {
		'label' : "Musical style"
	})}}

	{{form_row(numberForm.arrangers )}}
	<a href="{{ path('editorNewPerson') }}" target="_blank" title="Create a new person if not already in the database">add a person</a>

	{{form_row(numberForm.arrangerComment)}}
	{{form_row(numberForm.lyrics, { 'attr': {'class': 'materialize-textarea'}})}}

	{{ form_widget(numberForm.completeMusic) }}
	{{ form_label(numberForm.completeMusic, "Completed?") }}
	{{ form_errors(numberForm.completeMusic) }}

	{{form_row(numberForm.validationMusic)}}


</div>

<div class="cartouche">

	<h3>Intertextuality / References</h3>

	{{form_row(numberForm.source_thesaurus, {
		'label' : "Source (from thesaurus) (mandatory) (multi)"
	})}}
	{{form_row(numberForm.quotation_thesaurus, {
		'label' : "Quotation (1 choice)"
	})}}
	{{form_row(numberForm.quotation_text)}}

	{{ form_widget(numberForm.completeReference) }}
	{{ form_label(numberForm.completeReference, "Completed?") }}
	{{ form_errors(numberForm.completeReference) }}

	{{form_row(numberForm.validationReference)}}

</div>

<div class="cartouche">

	<h3>Complement</h3>

	{{form_row(numberForm.director)}}
	<a href="{{ path('editorNewPerson') }}" target="_blank" title="Create a new person if not already in the database">add a person</a>
	{{form_row(numberForm.cost)}}
	{{form_row(numberForm.costComment, { 'attr': {'class': 'materialize-textarea'}}) }}

	{{ form_widget(numberForm.completeCost) }}
	{{ form_label(numberForm.completeCost, "Completed?") }}
	{{ form_errors(numberForm.completeCost) }}

	{{form_row(numberForm.validationCost)}}

</div>


	<div class="cartouche">
	<h3>Others</h3>
	{{form_row(numberForm.timestamp)}}

</div>


