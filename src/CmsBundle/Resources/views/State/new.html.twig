{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
    <h1>State creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" class="btn black" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('admin_state_index') }}">Back to the list</a>
        </li>
    </ul>
    </div>
{% endblock %}
